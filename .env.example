# Neon PostgreSQL
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/desolate_bond?sslmode=require

# JWT
JWT_SECRET=change_me_in_production

# bcrypt 雜湊成本（輪次）。未設定預設 12。合法範圍 10–15。
# 每 +1 雜湊耗時翻倍；上線前可視伺服器 CPU 能力調整。
BCRYPT_ROUNDS=12

# Server
PORT=2567
NODE_ENV=development
